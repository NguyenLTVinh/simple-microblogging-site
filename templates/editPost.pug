include navbar.pug
doctype html
html(lang="en")
    head
        title Edit Post
    body
        +navbar(userId)
        h1 Edit Post
        form(method="post", action=`/api/edit-post/${post.id}`)
            textarea(name="content", required)= post.content
            button(type="submit") Update

